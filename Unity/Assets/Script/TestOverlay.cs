using UnityEngine;
public class TestOverlay : MonoBehaviour
{
    void Awake()
    {
        // print(Screen.width + " px");
        // print(gameObject.GetComponent<RectTransform>().sizeDelta + " units");
        // print(gameObject.GetComponent<RectTransform>().rect.width + " units");
        // gameObject.GetComponent<RectTransform>().sizeDelta.x;
        // RectTransform temp = gameObject.GetComponent<RectTransform>();
        // temp.rect = new Rect(temp.rect.x, temp.rect.y, temp.rect.width, temp.rect.width);
    }
}